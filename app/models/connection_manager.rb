class ConnectionManager

#   @user_list = []
#   @current_user_list = []

#   @users = User.all

#   @users.each do |user|
#     if user.followed_by?(current_user)
#       user_list << user
#     end

#   @users.each do |user|
#     if current_user.followed_by(user)
#       current_user_list << user
#     end

#   @users.each do |user|
#     if current_user_list.include(user) && user_list.include(user)
#     end
#   end
# end

end