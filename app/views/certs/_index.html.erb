<strong><%= cert.title %></strong>

<% if @user == current_user %>
  <small class="text-muted"><%= link_to 'Edit', edit_cert_path(cert), remote: true %> | </small>
  <small class="text-muted"><%= link_to 'Delete', cert, method: :delete %> </small>
<% end %><br>

&nbsp;&nbsp;&nbsp;Issued: <%= cert.start_date %><br>
&nbsp;&nbsp;&nbsp;Expires: <%= cert.expire_date %><br>
&nbsp;&nbsp;<strong> Description: </strong><br>
&nbsp;&nbsp;&nbsp;<%= cert.description %><br>
&nbsp;&nbsp; <%= link_to 'See Attachment', cert.attachment.url, target: '_blank' if cert.attachment_file_name %>
<hr>
